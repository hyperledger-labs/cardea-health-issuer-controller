{
  "defaultEnv":"test",
  "test": {
    "driver": "pg",
    "host": {"ENV": "DB_HOST"},
    "schema": "public",
    "database": {"ENV": "DB"},
    "user": {"ENV": "DB_USERNAME"},
    "password": {"ENV": "DB_PASSWORD"}
  }
}
